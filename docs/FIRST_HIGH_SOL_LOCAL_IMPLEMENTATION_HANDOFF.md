# Implementation handoff

Implement the bounded diagnostic specified by the authoritative design in `xmarre/ComfyUI-Sol-H3`, commit `0b8715faa0a82c730f5aaf0e44b1185e64291e49`, path `docs/FIRST_HIGH_SOL_LOCAL_ROOT_CAUSE_AND_FIX_DESIGN.md`:
https://github.com/xmarre/ComfyUI-Sol-H3/blob/0b8715faa0a82c730f5aaf0e44b1185e64291e49/docs/FIRST_HIGH_SOL_LOCAL_ROOT_CAUSE_AND_FIX_DESIGN.md

Read the complete design first. R/W are completed evidence; do not repeat them without a concrete validity defect. Their controlled result isolates the Sol-local numerical path. The ordinal Q/K neighbor-coordinate mismatch is verified in source, but media causality is unproven.

Before editing, re-fetch live main, relevant PR heads/bases, reviews, CI and repository instructions across Sol, VDN and Flow. Reconcile installed ComfyUI/bridge bytes and the actual lazy-loaded SM120 kernel/compiler provenance. W's bypass manifests do not establish sparse-kernel provenance. Never blindly copy stale file/line details; justify and checkpoint any adaptation to changed source.

Use a separate implementation mirror. Preserve Flow #43, VDN #15 and Sol #11 as diagnostic evidence, their base/topology contracts, Flow #33/#35/#36/#41 and the relevant Sol/VDN stacks. Do not create replacement PRs for existing work. Checkpoint to GitHub before destructive/risky work and after substantial progress.

Implement E first: one first-high H3 call from the preserved R bundle, fixed restricted VDN geometry/complement, real packaged SM120 all-selected local attention, 528 Sol local + 22 native dense-layer local + 50 native global + 100 native anchor receipts. Preserve schedule, inputs, gates, adapters and cleanup. Add the bounded same-input operator witnesses at block 2/groups 0,2,10, frozen-route streaming reference, provenance, fail-closed validity and separate diagnostic reporting exactly as specified. Sidecars add zero H3 calls and cannot mutate production state or receipts. Follow the design's decision table; run only the justified conditional follow-up.

Preserve R/W and new costly evidence durably. Production ownership and promotion remain gated on causal CUDA/operator/media results, then the specified trajectory regression. Do not substitute broad Sol disable, full attention, removed complement, dense warmup, extra H3 calls, state-transport/scheduler/Spectrum changes, resurrected #39/#40, or unrelated Weighted Mixed-Grid work because they are easier.
